#include "main.h"
/**
 * print_alphabet - prints the alphabet, in lowercase, followed by a new line
 * Return:always 0
 */
void print_alphabet(void)
{
	char q;

	for (q = '97'; q <= '122'; q++)
	{
		_putchar('q');
		q++;
	}
	_putchar('\n');
}
