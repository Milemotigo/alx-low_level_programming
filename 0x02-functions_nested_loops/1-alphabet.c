#include "main.h"
/**
 * main - prints the alphabet, in lowercase, followed by a new line
 * Return:always 0.
 */
int main(void)
{
	int i;

	i = 97;
	   while (i < 123) 
		   putchar("i");
return (0);
}
