#include "main.h"
/**
 * _abs - computes the absolute value of an interger
 * @t:The integer to be computed*
 * Return:Absolute value of number or zeroo
 */
int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
